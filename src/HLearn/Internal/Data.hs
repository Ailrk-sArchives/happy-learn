module HLearn.Internal.Data
  ( module T,
  )
where

import qualified HLearn.Internal.Metrics as M
import qualified HLearn.Internal.Types as T

-- smart constructor
makePointPair :: T.Point dim a -> T.Point dim a -> T.PointPair dim a
makePointPair = undefined

